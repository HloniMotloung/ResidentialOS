services:
  - type: web
    name: residentialos
    runtime: python
    buildCommand: >
      pip install -r requirements/production.txt &&
      python manage.py collectstatic --no-input &&
      python manage.py migrate &&
      echo "from apps.authentication.models import User; User.objects.create_superuser('admin', 'maclucia340@gmail.com', 'Dk34209Ano')" | python manage.py shell
    buildCommand: >
      pip install -r requirements/production.txt &&
      python manage.py collectstatic --no-input &&
      python manage.py migrate
      python manage.py createsu
    startCommand: gunicorn config.wsgi:application --workers 2 --timeout 120
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings.production
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: ".onrender.com"
      - key: DEFAULT_FROM_EMAIL
        value: noreply@tloupropertysolutions.co.za
      - key: DATABASE_URL
        fromDatabase:
          name: residentialos-db
          property: connectionString
      - key: PYTHON_VERSION
        value: "3.10.8"

databases:
  - name: residentialos-db
    plan: free
